<div>
    {{#if user}}
    <h5>Tu perfil (sin datos sensibles)</h5>
    <p>{{user.name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    {{else}}
    <h1>Inicia sesión para ver tu perfil</h1>
    <a href="/users/login">Ingresar</a>
    {{/if}}
</div>